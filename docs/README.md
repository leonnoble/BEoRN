# Building BEoRN Documentation Locally

This directory contains the Sphinx documentation for **BEoRN**.

## Requirements

```bash
pip install -r requirements.txt
```

## Build HTML docs

```bash
make html
```

To clean the previous build, one can run `make clean`. The output will be in `_build/html/index.html`.
